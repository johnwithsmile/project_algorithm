/* 

문자열을 정수로 바꾸기
https://school.programmers.co.kr/learn/courses/30/lessons/12925

*/

function solution(s) {
  let answer = 0;
  answer = Number(s);
  return answer;
}

/* 

  문자열을 숫자로 바꾸기
  Number를 쓰면 숫자로 바뀐다.

  베스트 풀이를 보면 이렇게도 푼다. 1로 나뉘면 숫자로 바뀌어버리는것
  조금 더 생각했으면 생각났을텐데
  function strToInt(str){
  return str/1
  }

  
 */
